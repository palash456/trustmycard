# @trustmycard/marketing

Optional static marketing site (Hostinger `www` deploy). CTAs link to the wallet app homepage (`/`).

For day-to-day dev, run the wallet app — the full homepage lives at `/`:

```bash
cd frontend
npm run dev:website     # http://localhost:3000
```
