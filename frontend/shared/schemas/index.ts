/**
 * Shared Zod (or similar) schemas for request/response validation.
 * Backend and frontend can both import these to stay in sync.
 *
 * Add schemas as the API contract stabilizes, e.g.:
 *   prepareApprovalSchema, confirmApprovalSchema, …
 */

export {};
