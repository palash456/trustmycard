# Domain migration (moved)

This guide is now **domain-agnostic**. See:

**[domain-migration.md](./domain-migration.md)**

Use example placeholders (`old-domain.example`, `new-domain.example`) in the doc; enter your real domains in the admin **Run migration test suite** modal.
