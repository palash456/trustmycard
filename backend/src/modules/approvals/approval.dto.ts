/** Request/response DTOs — keep aligned with @trustmycard/shared types/schemas.
 * Domain persistence shape lives in prisma/schema.prisma (no separate entity file).
 */
export class PrepareApprovalDto {}
export class ConfirmApprovalDto {}
