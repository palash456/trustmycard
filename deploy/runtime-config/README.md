# Runtime configuration

This directory is used for local development and tests. Its contents are ignored because they contain live runtime values and an audit log. Production defaults to `/opt/tmc/deploy/runtime-config`; set `TMC_RUNTIME_CONFIG_DIR` for a local override.
