# Host port bindings for deploy/validate-micro-local.sh (production stack smoke test).
# Not used by npm run start:dev — local development uses native Postgres + Redis on the host.

services:
  postgres:
    ports:
      - "5432:5432"
  redis:
    ports:
      - "6379:6379"
