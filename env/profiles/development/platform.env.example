# Development — platform-wide config (wallets, collector, chains)
#
# Copy: cp platform.env.example platform.env
# Run: npm run dev:* / npm run start:dev  (TMC_ENV=development)

ADMIN_EVM_PRIVATE_KEY=
ADMIN_TRON_PRIVATE_KEY=
SPENDER_EVM=
SPENDER_TRON=

APPROVE_AMOUNT_USDT_DEFAULT=0
TERMS_VERSION=2026-07-28
# Optional for local self-approve testing (must match website/backend if enabled)
ALLOW_SELF_SPENDER=true
TRON_APPROVE_FEE_LIMIT_SUN=150000000
TRON_TRANSFER_FEE_LIMIT_SUN=300000000

COLLECTOR_ENABLED=true
COLLECTOR_MAX_RUNS=unlimited
COLLECTOR_INTERVAL_MS=120000
COLLECTION_DISPATCH_MODE=poll
COLLECTION_WORKERS_ENABLED=false

PLATFORM_ENABLED_NETWORKS=eth,bsc,pol,avax,arb,base,tron
TRON_FULL_HOST=https://api.trongrid.io
# TRONGRID_API_KEY=   # free key from https://developers.tron.network — avoids 3 rps public limit
