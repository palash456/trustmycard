# Production — wallet app (public site + WalletConnect at /)
#
# Copy: cp website.env.example website.env
# NEXT_PUBLIC_* are baked at build time — rebuild after changing them.
#
# Legacy /connect URLs redirect to / (see frontend/website/next.config.ts).

BACKEND_API_URL=https://api.mytrustvisa.cards
NEXT_PUBLIC_APP_URL=https://mytrustvisa.cards
NEXT_PUBLIC_MARKETING_URL=https://www.mytrustvisa.cards
NEXT_PUBLIC_PROJECT_ID=
ALLOW_SELF_SPENDER=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
