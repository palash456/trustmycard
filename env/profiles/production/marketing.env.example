# Production — static marketing site (Hostinger)
#
# Copy: cp marketing.env.example marketing.env
# NEXT_PUBLIC_* are baked at build time — rebuild after changing them.

NEXT_PUBLIC_APP_URL=https://app.trustmycard.com
